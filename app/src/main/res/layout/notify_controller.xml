<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:weightSum="1"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/notify_skip_previous"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentStart="true"
        android:src="@mipmap/skip_previous_round" />

    <ImageView
        android:id="@+id/notify_start_pause"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_toEndOf="@id/notify_skip_previous"
        android:layout_marginStart="20dp"
        android:src="@mipmap/pause_round" />

    <ImageView
        android:id="@+id/notify_skip_next"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_toEndOf="@id/notify_start_pause"
        android:layout_marginStart="20dp"
        android:src="@mipmap/skip_next_round" />

</RelativeLayout>